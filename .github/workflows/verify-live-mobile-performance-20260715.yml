name: Verify live mobile performance

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/verify-live-mobile-performance-20260715.yml"
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

jobs:
  lighthouse:
    if: >-
      github.event_name == 'push' ||
      (github.event.issue.number == 375 &&
       github.event.comment.body == '/verify-mobile-performance' &&
       github.event.comment.user.login == 'irhaapparelsofficial-ctrl')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Run detailed mobile Lighthouse against live Germany page
        shell: bash
        run: |
          set -euo pipefail
          npx --yes lighthouse@12.8.2 \
            "https://irhaapparels.com/de/bekleidungshersteller-deutschland?lh_detail=${GITHUB_SHA::12}" \
            --output=json \
            --output-path=/tmp/lighthouse.json \
            --only-categories=performance,seo,accessibility,best-practices \
            --form-factor=mobile \
            --screenEmulation.mobile=true \
            --throttling-method=simulate \
            --chrome-flags="--headless --no-sandbox --disable-dev-shm-usage"

          node <<'NODE'
          const fs = require('fs');
          const report = JSON.parse(fs.readFileSync('/tmp/lighthouse.json', 'utf8'));
          const audits = report.audits || {};
          const score = (id) => Math.round((report.categories[id]?.score ?? 0) * 100);
          const metric = (id) => audits[id]?.displayValue ?? 'n/a';
          const kb = (bytes) => `${Math.round((bytes || 0) / 1024)} KiB`;
          const clean = (value) => String(value ?? '').replace(/\|/g, '\\|').replace(/\s+/g, ' ').trim();

          const unusedJs = (audits['unused-javascript']?.details?.items || [])
            .map((item) => ({ url: item.url, total: item.totalBytes, waste: item.wastedBytes }))
            .sort((a, b) => (b.waste || 0) - (a.waste || 0))
            .slice(0, 12);
          const unusedCss = (audits['unused-css-rules']?.details?.items || [])
            .map((item) => ({ url: item.url, total: item.totalBytes, waste: item.wastedBytes }))
            .sort((a, b) => (b.waste || 0) - (a.waste || 0))
            .slice(0, 8);
          const network = (audits['network-requests']?.details?.items || [])
            .map((item) => ({ url: item.url, transfer: item.transferSize, type: item.resourceType }))
            .sort((a, b) => (b.transfer || 0) - (a.transfer || 0))
            .slice(0, 15);
          const lcpItem = audits['largest-contentful-paint-element']?.details?.items?.[0]?.items?.[0]
            || audits['largest-contentful-paint-element']?.details?.items?.[0]
            || null;
          const lcpSnippet = lcpItem?.node?.snippet || lcpItem?.snippet || 'not exposed';
          const insightIds = Object.keys(audits)
            .filter((id) => /(lcp|render-block|font|server-response|document-latency|network-dependency)/i.test(id))
            .sort();

          let markdown = '## Detailed live mobile Lighthouse — Germany SEO page\n\n';
          markdown += `Release tested: \`${report.finalDisplayedUrl}\`\n\n`;
          markdown += [
            `Performance: ${score('performance')}`,
            `Accessibility: ${score('accessibility')}`,
            `Best Practices: ${score('best-practices')}`,
            `SEO: ${score('seo')}`,
            `FCP: ${metric('first-contentful-paint')}`,
            `LCP: ${metric('largest-contentful-paint')}`,
            `TBT: ${metric('total-blocking-time')}`,
            `CLS: ${metric('cumulative-layout-shift')}`,
            `Speed Index: ${metric('speed-index')}`,
            `TTFB/server response: ${metric('server-response-time')}`,
            `Main-thread work: ${metric('mainthread-work-breakdown')}`,
            `JavaScript execution: ${metric('bootup-time')}`,
            `Total transfer: ${metric('total-byte-weight')}`,
          ].map((x) => `- ${x}`).join('\n');
          markdown += `\n\n### LCP element\n\n\`${clean(lcpSnippet).slice(0, 700)}\`\n`;
          markdown += '\n### Relevant audits\n\n';
          markdown += insightIds.map((id) => {
            const audit = audits[id];
            const ms = audit?.details?.overallSavingsMs;
            const bytes = audit?.details?.overallSavingsBytes;
            const savings = [
              Number.isFinite(ms) ? `${Math.round(ms)} ms` : '',
              Number.isFinite(bytes) ? kb(bytes) : '',
            ].filter(Boolean).join(', ');
            return `- \`${id}\`: ${clean(audit?.title)} — ${clean(audit?.displayValue)}${savings ? `; saving ${savings}` : ''}`;
          }).join('\n') || '- None exposed';
          markdown += '\n\n### Largest unused JavaScript files\n\n| Resource | Total | Saving |\n|---|---:|---:|\n';
          markdown += unusedJs.map((x) => `| ${clean(x.url).slice(-120)} | ${kb(x.total)} | ${kb(x.waste)} |`).join('\n') || '| none | 0 | 0 |';
          markdown += '\n\n### Largest unused CSS files\n\n| Resource | Total | Saving |\n|---|---:|---:|\n';
          markdown += unusedCss.map((x) => `| ${clean(x.url).slice(-120)} | ${kb(x.total)} | ${kb(x.waste)} |`).join('\n') || '| none | 0 | 0 |';
          markdown += '\n\n### Largest network transfers\n\n| Resource | Type | Transfer |\n|---|---|---:|\n';
          markdown += network.map((x) => `| ${clean(x.url).slice(-120)} | ${clean(x.type)} | ${kb(x.transfer)} |`).join('\n') || '| none | — | 0 |';
          markdown += '\n';

          console.log(markdown);
          fs.writeFileSync('/tmp/lighthouse-summary.md', markdown);
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, markdown);
          NODE

      - name: Upload Lighthouse JSON
        uses: actions/upload-artifact@v4
        with:
          name: detailed-live-mobile-lighthouse
          path: /tmp/lighthouse.json
          retention-days: 3

      - name: Record audit evidence
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh issue comment 375 --repo "$GITHUB_REPOSITORY" --body-file /tmp/lighthouse-summary.md
