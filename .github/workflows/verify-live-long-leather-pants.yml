name: Verify Live Long Leather Pants

on:
  push:
    branches:
      - verify-live-long-leather-pants-post-publish
    paths:
      - "scripts/verify-live-long-leather-pants.mjs"
      - ".github/workflows/verify-live-long-leather-pants.yml"
  pull_request:
    branches:
      - main
    paths:
      - "scripts/verify-live-long-leather-pants.mjs"
      - ".github/workflows/verify-live-long-leather-pants.yml"

permissions:
  contents: read

jobs:
  live-browser-check:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: verify-live-long-leather-pants-post-publish

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Playwright
        run: |
          npm install --no-save --legacy-peer-deps --package-lock=false playwright@1.56.1
          npx playwright install --with-deps chromium

      - name: Verify live published routes
        run: node scripts/verify-live-long-leather-pants.mjs

      - name: Upload live report and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-long-leather-pants-verification
          path: artifacts/live-long-leather-pants
          if-no-files-found: error
          retention-days: 7
