name: IA SEC E003 Finalize Corrections

on:
  push:
    branches:
      - sec/ia-sec-e003-durable-rate-limits
    paths:
      - scripts/ci/ia-sec-e003-finalize.py
      - .github/workflows/ia-sec-e003-finalize.yml
  pull_request:
    branches:
      - main
    types: [synchronize]
    paths:
      - scripts/ci/ia-sec-e003-finalize.py
      - .github/workflows/ia-sec-e003-finalize.yml

permissions:
  contents: write

concurrency:
  group: ia-sec-e003-finalize-corrections
  cancel-in-progress: false

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sec/ia-sec-e003-durable-rate-limits
          fetch-depth: 0

      - name: Apply consistency corrections
        run: python3 scripts/ci/ia-sec-e003-finalize.py

      - name: Remove temporary controls and commit
        run: |
          rm scripts/ci/ia-sec-e003-finalize.py
          rm .github/workflows/ia-sec-e003-finalize.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "security: preserve anonymous limiter continuity"
          git push origin HEAD:sec/ia-sec-e003-durable-rate-limits
