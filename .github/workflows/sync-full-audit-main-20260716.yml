name: Sync full audit branch with latest main

on:
  push:
    branches: [fix/full-audit-hardening-20260716]
    paths:
      - ".github/workflows/sync-full-audit-main-20260716.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/full-audit-hardening-20260716
          fetch-depth: 0
      - name: Merge exact latest main and remove bootstrap
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
          rm .github/workflows/sync-full-audit-main-20260716.yml
          git add -A
          git commit -m "chore: remove one-time branch sync workflow"
          git push origin HEAD:fix/full-audit-hardening-20260716
