name: IA Media E001 Recovery Controller V3 Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired controller
        run: echo "Retired. Use ia-media-e001-recovery-controller.yml only."
