name: Public Chat Test Diagnostic

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Capture test output
        shell: bash
        run: |
          set +e
          npm test -- --run > public-chat-test.log 2>&1
          code=$?
          echo "$code" > public-chat-test-exit-code.txt
          tail -n 500 public-chat-test.log
          exit 0
      - name: Upload diagnostic log
        uses: actions/upload-artifact@v4
        with:
          name: public-chat-test-log
          path: |
            public-chat-test.log
            public-chat-test-exit-code.txt
          retention-days: 1
