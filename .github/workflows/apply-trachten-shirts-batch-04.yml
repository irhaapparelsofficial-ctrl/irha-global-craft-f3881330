name: Apply Trachten Shirts Batch 04

on:
  push:
    branches:
      - trachten-shirts-batch-04
    paths:
      - "scripts/patch_add_batch09_catalog.py"
      - ".github/workflows/apply-trachten-shirts-batch-04.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out working branch
        uses: actions/checkout@v4
        with:
          ref: trachten-shirts-batch-04
          fetch-depth: 0

      - name: Apply Batch 09 catalog registration
        run: python scripts/patch_add_batch09_catalog.py

      - name: Validate Batch 09 media manifest
        run: python -m json.tool scripts/product-media-batch-09.json >/dev/null

      - name: Commit registration and remove one-time files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/apply-trachten-shirts-batch-04.yml
          git rm scripts/patch_add_batch09_catalog.py
          git add src/hooks/usePublicCatalog.ts
          git commit -m "Register Trachten Shirts batch 04"
          git push origin HEAD:trachten-shirts-batch-04
