name: Mobile Polish Test Diagnostic

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Capture test output
        shell: bash
        run: |
          set +e
          npm test -- --run > mobile-polish-test.log 2>&1
          code=$?
          echo "$code" > mobile-polish-exit-code.txt
          tail -n 500 mobile-polish-test.log
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-polish-test-log
          path: |
            mobile-polish-test.log
            mobile-polish-exit-code.txt
          retention-days: 1
