name: Apply Lead Activation Failure Checkpoint

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Record disabled one-shot patcher
        run: |
          echo "The recursive branch push patcher is disabled."
          echo "The recovery checkpoint is maintained directly in source and verified by Quality Gate."
