# IA-MEDIA-E001 Immutable Staging Evidence

## Execution lock

- Execution ID: `IA-MEDIA-E001`
- Goal lock: `IRHA-B2B-VISUAL-RECOVERY-01`
- Product scope: `IRHA-P001` through `IRHA-P007`
- Media version: `ia-media-e001-20260730`
- Supabase project: `pvzjiozismyxqrzmtfbi`
- Bucket: `site-media`

## Source and processing contract

Every selected source is tied to its exact Google Drive file ID and product folder in `ops/ia-media-e001/media-plan.mjs`. The preserved `catalog-originals` object is SHA-256 checked before decoding and processing. Candidate roles and product identity are based on visual inspection, construction evidence, source grouping and checksums rather than filename order alone.

The immutable destination root is:

```text
catalog/recovery/ia-media-e001-20260730/
```

No existing buyer-facing object was overwritten in place.

## Staged object inventory

Supabase Storage verification after the guarded staging run returned:

| Tier | Objects |
|---|---:|
| Canonical WebP | 41 |
| 360 px | 41 |
| 720 px | 41 |
| 1200 px | 41 |
| 1600 px | 41 |
| **Total** | **205** |

Additional Storage assertions:

- MIME mismatches: `0`
- zero-byte objects: `0`
- retired 480 px objects generated by this execution: `0`
- retired 2400 px objects generated by this execution: `0`
- database mutation during staging: `false`

Every staged object was re-downloaded and checked for:

- WebP decoding;
- expected width;
- non-zero dimensions;
- exact generated SHA-256 checksum;
- immutable-path collision safety.

## Database boundary

The staging phase did not update `public.products` or `public.catalog_drive_files`. Product/gallery and provenance updates remain a separate atomic operation after official-logo preview acceptance. A rollback snapshot covering all P001–P007 product rows, the unpublished P002 duplicate, the 41 selected source rows and the rejected P003 checksum duplicate was captured before any database mutation.

## Rejected candidates

- Drive file `1ofqBrOVRTPUWmGeofRRw1ER5hwC93qWF` was rejected as the official logo because pixel review showed a shield/IA mark, not the approved navy-and-gold circular Irha Apparels Manufacturing Specialists crest.
- Drive file `1N2HfKQMsBuAQSUMjJXuKVLjPlfUAdSG3` remains unpublished because its checksum is identical to the selected P003 front-three-quarter source.
- P001 did not receive a fabricated rear-three-quarter role: the filename-labelled candidate visibly showed the front bib and was classified as an alternate front-three-quarter image.

## Release order

1. Prove the official owner logo on the exact immutable preview.
2. Apply the atomic P001–P007 database/provenance mapping to the already verified immutable objects.
3. Complete responsive preview acceptance for every image and thumbnail.
4. Merge only after required checks and preview evidence pass.
5. Deploy the exact immutable main artifact and repeat production verification.
